# Data is published as GitHub Releases, never committed
export/
site/data/
*.parquet
*.csv

# Licensed third-party inputs (e.g. commercial journal lists) must never be committed
*.xlsx
*.xls

# Local only
working-notes.md
.claude/
__pycache__/
